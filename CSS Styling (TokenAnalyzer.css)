/* Main Container */
.token-analyzer {
    max-width: 1400px;
    margin: 0 auto;
    padding: 20px;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
        'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
        sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
}

/* Header */
.app-header {
    text-align: center;
    margin-bottom: 40px;
    color: white;
}

.logo {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
}

.logo-icon {
    font-size: 48px;
}

.app-header h1 {
    font-size: 2.5rem;
    margin: 0;
    font-weight: 700;
}

.tagline {
    font-size: 1.1rem;
    opacity: 0.9;
    margin-top: 10px;
}

/* Search Section */
.search-section {
    background: white;
    border-radius: 16px;
    padding: 30px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
    margin-bottom: 30px;
}

.search-container {
    display: flex;
    gap: 15px;
    margin-bottom: 15px;
}

.token-input {
    flex: 1;
    padding: 16px 20px;
    border: 2px solid #e5e7eb;
    border-radius: 12px;
    font-size: 16px;
    transition: all 0.3s;
}

.token-input:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.analyze-button {
    padding: 16px 40px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    display: flex;
    align-items: center;
    gap: 8px;
}

.analyze-button:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
}

.analyze-button:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}

.spinner {
    width: 16px;
    height: 16px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top-color: white;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

/* Quick Tips */
.quick-tips {
    text-align: center;
    color: #6b7280;
    font-size: 14px;
}

/* Summary Banner */
.summary-banner {
    background: white;
    border-radius: 16px;
    padding: 30px;
    margin-bottom: 30px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
    border-left: 6px solid;
}

.summary-banner.green { border-left-color: #10b981; }
.summary-banner.yellow { border-left-color: #fbbf24; }
.summary-banner.orange { border-left-color: #f97316; }
.summary-banner.red { border-left-color: #ef4444; }

.banner-content {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 30px;
    align-items: center;
    margin-bottom: 20px;
}

.verdict-section {
    display: flex;
    align-items: center;
    gap: 20px;
}

.emoji-large {
    font-size: 64px;
}

.verdict-text h2 {
    margin: 0 0 5px 0;
    font-size: 1.8rem;
    color: #111827;
}

.verdict-text p {
    margin: 0;
    color: #6b7280;
}

/* Score Circle */
.score-section {
    text-align: center;
}

.score-circle {
    width: 120px;
    height: 120px;
    position: relative;
    margin: 0 auto 10px;
}

.score-circle svg {
    width: 100%;
    height: 100%;
}

.score-number {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
}

.score-value {
    font-size: 32px;
    font-weight: 700;
    color: #111827;
}

.score-max {
    font-size: 16px;
    color: #6b7280;
}

.score-label {
    font-weight: 600;
    color: #6b7280;
}

/* Quick Stats */
.quick-stats {
    display: flex;
    gap: 30px;
    justify-content: flex-end;
}

.stat {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.stat-label {
    font-size: 14px;
    color: #6b7280;
    margin-bottom: 5px;
}

.stat-value {
    font-size: 24px;
    font-weight: 700;
    color: #111827;
}

/* Key Points */
.key-points {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 12px;
}

.key-point {
    padding: 12px 16px;
    background: #f9fafb;
    border-radius: 8px;
    font-size: 14px;
    line-height: 1.5;
}

/* Content Grid */
.content-grid {
    display: grid;
    grid-template-columns: 400px 1fr;
    gap: 30px;
    margin-bottom: 30px;
}

/* Cards */
.card {
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    margin-bottom: 20px;
    overflow: hidden;
}

.card-header {
    padding: 20px 25px;
    border-bottom: 1px solid #e5e7eb;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.card-header h3 {
    margin: 0;
    font-size: 1.25rem;
    color: #111827;
}

.card-body {
    padding: 25px;
}

/* Badges */
.rating-badge, .status-badge, .action-badge {
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 600;
}

.rating-badge.green, .status-badge.bullish {
    background: #d1fae5;
    color: #065f46;
}

.rating-badge.yellow, .status-badge.neutral {
    background: #fef3c7;
    color: #92400e;
}

.rating-badge.orange, .rating-badge.red {
    background: #fee2e2;
    color: #991b1b;
}

/* Developer Card */
.developer-card.bullish {
    border-left: 4px solid #10b981;
}

.developer-card.bearish {
    border-left: 4px solid #ef4444;
}

.sell-progress {
    margin-bottom: 20px;
}

.progress-label {
    display: flex;
    justify-content: space-between;
    margin-bottom: 8px;
    font-size: 14px;
    font-weight: 600;
}

.progress-bar {
    height: 12px;
    background: #e5e7eb;
    border-radius: 6px;
    overflow: hidden;
}

.progress-fill {
    height: 100%;
    transition: width 0.5s ease;
}

.risk-box, .pattern-box {
    background: #f9fafb;
    border-radius: 8px;
    padding: 15px;
    margin: 15px 0;
}

.risk-header, .pattern-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 8px;
    font-weight: 600;
}

.risk-value.none, .risk-value.very-low {
    color: #10b981;
}

.risk-value.high {
    color: #ef4444;
}

.score-impact {
    display: flex;
    justify-content: space-between;
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px solid #e5e7eb;
}

.impact-value.positive {
    color: #10b981;
    font-weight: 700;
}

.impact-value.negative {
    color: #ef4444;
    font-weight: 700;
}

/* Chart */
.chart-container {
    position: relative;
}

.levels-display {
    display: flex;
    justify-content: space-between;
    margin-top: 20px;
    padding: 15px;
    background: #f9fafb;
    border-radius: 8px;
}

.level {
    display: flex;
    flex-direction: column;
    gap: 5px;
}

.level-label {
    font-size: 12px;
    color: #6b7280;
    text-transform: uppercase;
    font-weight: 600;
}

.level-price {
    font-size: 16px;
    font-weight: 700;
}

.level.resistance .level-price {
    color: #ef4444;
}

.level.support .level-price {
    color: #10b981;
}

.level.current .level-price {
    color: #3b82f6;
}

/* Signals */
.signal-summary {
    margin-bottom: 20px;
}

.score-bars {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.score-bar {
    display: grid;
    grid-template-columns: 80px 1fr 50px;
    gap: 10px;
    align-items: center;
}

.bar-container {
    height: 24px;
    background: #e5e7eb;
    border-radius: 12px;
    overflow: hidden;
}

.bar-fill {
    height: 100%;
    transition: width 0.5s ease;
}

.score-bar.bullish .bar-fill {
    background: linear-gradient(90deg, #10b981, #34d399);
}

.score-bar.bearish .bar-fill {
    background: linear-gradient(90deg, #ef4444, #f87171);
}

.bar-value {
    font-weight: 700;
    text-align: right;
}

.signals-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.signal-item {
    padding: 15px;
    border-radius: 8px;
    border-left: 4px solid;
}

.signal-item.buy {
    background: #d1fae5;
    border-left-color: #10b981;
}

.signal-item.sell {
    background: #fee2e2;
    border-left-color: #ef4444;
}

.signal-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 5px;
}

.signal-indicator {
    font-weight: 600;
    flex: 1;
}

.signal-badge {
    padding: 4px 8px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
}

.signal-badge.strong {
    background: #111827;
    color: white;
}

.signal-badge.medium {
    background: #6b7280;
    color: white;
}

.signal-reason {
    margin: 0;
    font-size: 14px;
    color: #4b5563;
}

/* Entry Card */
.current-action {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 20px;
    border-radius: 12px;
    margin-bottom: 20px;
}

.action-header {
    margin-bottom: 10px;
}

.confidence-badge {
    background: rgba(255, 255, 255, 0.2);
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 600;
}

.best-entry {
    background: #f9fafb;
    padding: 20px;
    border-radius: 12px;
    margin-bottom: 20px;
}

.best-entry h4 {
    margin-top: 0;
    color: #111827;
}

.entry-details {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin: 15px 0;
}

.entry-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.entry-label {
    font-weight: 600;
    color: #6b7280;
}

.entry-value {
    font-weight: 600;
    color: #111827;
}

.entry-value.price {
    color: #3b82f6;
}

.entry-value.stop-loss {
    color: #ef4444;
}

.tp-levels {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
}

.tp-level {
    padding: 4px 8px;
    background: #d1fae5;
    color: #065f46;
    border-radius: 6px;
    font-size: 13px;
    font-weight: 600;
}

/* Risk Management */
.risk-management {
    background: #fef3c7;
    padding: 20px;
    border-radius: 12px;
    border-left: 4px solid #f59e0b;
}

.risk-management h4 {
    margin-top: 0;
    color: #92400e;
}

.position-sizing, .tp-strategy {
    margin: 15px 0;
}

.section-label {
    display: block;
    font-weight: 600;
    margin-bottom: 10px;
    color: #78350f;
}

.sizing-options {
    display: flex;
    gap: 15px;
}

.sizing-option {
    flex: 1;
    background: white;
    padding: 12px;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    gap: 5px;
}

.option-label {
    font-size: 12px;
    color: #6b7280;
    font-weight: 600;
}

.option-value {
    font-weight: 700;
    color: #111827;
}

.tp-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
}

.tp-item {
    background: white;
    padding: 12px;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    gap: 5px;
}

.tp-name {
    font-size: 12px;
    font-weight: 600;
    color: #6b7280;
}

.tp-price {
    font-weight: 700;
    color: #10b981;
}

.tp-sell {
    font-size: 13px;
    color: #4b5563;
}

/* Responsive */
@media (max-width: 1200px) {
    .content-grid {
        grid-template-columns: 1fr;
    }
    
    .banner-content {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .search-container {
        flex-direction: column;
    }
    
    .key-points {
        grid-template-columns: 1fr;
    }
    
    .sizing-options {
        flex-direction: column;
    }
    
    .tp-grid {
        grid-template-columns: 1fr;
    }
}
